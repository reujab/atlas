[Unit]
Description=Geolocation daemon
Before=postgresql.service
After=networking.target

[Service]
Type=oneshot
ExecStartPre=/bin/sh -c 'until ping -c1 -W1 google.com > /dev/null; do sleep 1; done; echo ready; sleep 1'
ExecStart=/usr/local/bin/geod

[Install]
WantedBy=graphical.target
